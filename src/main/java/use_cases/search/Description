Processes the use case of searching stocks with ticker symbol

Input:
    String symbol: a string entered in the search bar, automatically stripped and converted to upper case

Output: a list of stocks with symbols similar to the input symbol
    ViewStockOutputData searchOutputData

    // Contains the following
    // String input: the user input
    // List<String> symbols: the list of ticker symbols obtained from the input
